name: Deploy → QA

on:
  push:
    tags: ['qa-*']

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: qa
      railway_environment: qa
      ref: ${{ github.sha }}
    secrets: inherit
