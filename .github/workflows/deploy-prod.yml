name: Deploy → Prod

on:
  push:
    tags: ['v*']

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: prod
      railway_environment: production
      ref: ${{ github.sha }}
    secrets: inherit
