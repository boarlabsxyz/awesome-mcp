name: Deploy → Staging

on:
  workflow_dispatch:
    inputs:
      ref:
        description: Git SHA to deploy
        required: true
        type: string

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: staging
      railway_environment: staging
      ref: ${{ inputs.ref }}
    secrets: inherit
